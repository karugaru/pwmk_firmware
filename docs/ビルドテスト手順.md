# ビルドテスト手順

## はじめに

このガイドでは、Docker を使って CLI ビルドを検証する手順を説明します。
ビルド本体は [tools/build.py](../tools/build.py) が行い、この手順ではその実行をコンテナ内で自動化します。

## 1. 前提環境

この手順は次の前提で動作します。

- Linux 環境に入っていること
- Dockerと`python3` が使えること

ビルドテストでは APT パッケージ取得を高速化するために、`apt-cacher-ng` のコンテナを自動的に起動して利用します。

## 2. ビルドテストの実行

リポジトリルートで次のコマンドを実行します。

```bash
python3 tools/test_build.py
```

実行を開始すると確認なしでビルドが進行するのでご注意ください。

このスクリプトは次の処理を行います。

- `pwmk-build-test-network` ネットワークを作成し、必要であれば `pwmk-apt-cacher-ng` コンテナを起動する
- 公式の `ubuntu:24.04` コンテナを起動する
- ソースツリーを `/workspace` にマウントする
- コンテナ内の APT を `apt-cacher-ng` 経由に設定する
- コンテナ内で `python3` を導入する
- コンテナ内でビルドを実行する

## 3. 補足

APT キャッシュを消したい場合は、必要に応じて Docker 側で `pwmk-apt-cacher-ng` コンテナと `pwmk-apt-cacher-ng-cache` ボリュームを削除してください。

## 4. 期待する結果

ビルドが成功すると、コンテナ内のログ末尾で `pwmk.elf` のリンク完了とメモリ使用量が表示されます。
