[project]
name = "pwmk-firmware-tools"
version = "0.1.0"
description = "Build and test helper scripts for pwmk_firmware"
requires-python = ">=3.11"
dependencies = ["typer>=0.16,<1", "python-on-whales>=0.81,<1"]

[tool.uv]
package = false
